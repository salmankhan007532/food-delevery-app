import 'package:flutter/material.dart';

class IngredientesContainer extends StatelessWidget {
  const IngredientesContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      
    );
  }
}